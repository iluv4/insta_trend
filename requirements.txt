fastapi>=0.115,<0.137
uvicorn[standard]>=0.30,<0.50
httpx>=0.27,<0.28
SQLAlchemy>=2.0,<2.1
APScheduler>=3.10,<4.0
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0,<2.0
numpy>=1.26
pandas>=2.2

# Postgres driver (psycopg 3) — used when DATABASE_URL points at Postgres in
# production; SQLite (the default) needs no extra driver.
psycopg[binary]>=3.1,<4.0

# dev / test
pytest>=8.0
