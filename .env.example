# ── Instagram data source (RapidAPI) ─────────────────────────────────────────
# Sign up at rapidapi.com and subscribe to the instagram120 API, then paste the
# key here. Without it the API/collector still run but live collection returns
# a 502 (use scripts/seed_demo.py to explore with synthetic data).
RAPIDAPI_KEY=
RAPIDAPI_HOST=instagram120.p.rapidapi.com

# ── Storage ──────────────────────────────────────────────────────────────────
# SQLite by default; point at Postgres for production, e.g.
# postgresql+psycopg://user:pass@host:5432/dbname
DATABASE_URL=sqlite:///./insta_monitor.db

# ── Scheduler ────────────────────────────────────────────────────────────────
COLLECT_INTERVAL_MINUTES=360
ENABLE_SCHEDULER=true

# ── Analysis defaults ────────────────────────────────────────────────────────
DEFAULT_FREQ=1D
DEFAULT_FORECAST_HORIZON=7
